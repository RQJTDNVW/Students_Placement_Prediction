import type { StudentInput } from '../../types/prediction.ts'
import { clamp } from '../../utils/format.ts'
import { Card, SectionHeader } from '../common/Card.tsx'
import { ProgressBar } from '../common/ProgressBar.tsx'

function average(values: number[]) { return values.reduce((sum, value) => sum + value, 0) / values.length }
export function getProfileIndicators(input: StudentInput) { return [{ label: 'Academic strength', value: average([input.cgpa / 10 * 100, input.aptitude_score]), details: `CGPA ${input.cgpa.toFixed(1)} · Aptitude ${input.aptitude_score}/100` }, { label: 'Experience', value: average([clamp(input.internships / 3 * 100), clamp(input.projects_count / 6 * 100), clamp(input.certifications / 4 * 100)]), details: `${input.internships} internships · ${input.projects_count} projects · ${input.certifications} certifications` }, { label: 'Technical / aptitude', value: input.aptitude_score, details: `Aptitude score ${input.aptitude_score}/100` }, { label: 'Communication', value: input.communication_skills * 10, details: `Communication ${input.communication_skills}/10` }, { label: 'Extracurricular involvement', value: input.extracurricular_activities * 10, details: `Activities ${input.extracurricular_activities}/10` }] }
export function ProfileIndicators({ input }: { input: StudentInput }) { const indicators = getProfileIndicators(input); return <Card className="p-5 sm:p-6"><SectionHeader eyebrow="Transparent review" title="Student Profile Indicators" description="A normalized summary of submitted values for profile review. These bars are not SHAP values or model feature importance." /><div className="space-y-5">{indicators.map((indicator) => <div key={indicator.label}><div className="mb-2 flex flex-wrap items-center justify-between gap-2"><span className="text-sm font-semibold text-ink">{indicator.label}</span><span className="mono text-xs text-ink">{Math.round(indicator.value)}%</span></div><ProgressBar value={indicator.value} showValue={false} /><p className="mt-2 text-xs text-meta">{indicator.details}</p></div>)}</div></Card> }
