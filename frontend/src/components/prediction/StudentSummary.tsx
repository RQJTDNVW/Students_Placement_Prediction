import type { PredictionResponse, StudentInput } from '../../types/prediction.ts'
import { formatDateTime, formatProbability } from '../../utils/format.ts'
import { RiskBadge, StatusMarker } from '../common/Badge.tsx'
import { Card, SectionHeader } from '../common/Card.tsx'

const fields: Array<{ label: string; key: keyof StudentInput }> = [{ label: 'CGPA', key: 'cgpa' }, { label: 'Aptitude score', key: 'aptitude_score' }, { label: 'Internships', key: 'internships' }, { label: 'Projects', key: 'projects_count' }, { label: 'Certifications', key: 'certifications' }, { label: 'Communication', key: 'communication_skills' }, { label: 'Extracurricular', key: 'extracurricular_activities' }]
export function StudentSummary({ input, result }: { input: StudentInput; result: PredictionResponse }) { return <Card className="print-surface p-5 sm:p-6"><SectionHeader eyebrow="Prediction record" title="Student input summary" description="Values submitted for this model request." /><div className="grid gap-3 sm:grid-cols-4">{fields.map(({ label, key }) => <div key={key} className="border-b border-line pb-3"><p className="eyebrow text-[9px]">{label}</p><p className="mono mt-2 text-sm text-ink">{input[key]}</p></div>)}</div><div className="mt-6 grid gap-3 border-t border-line pt-5 sm:grid-cols-4"><div><p className="eyebrow text-[9px]">Result</p><div className="mt-2"><StatusMarker status={result.status} /></div></div><div><p className="eyebrow text-[9px]">Probability</p><p className="mono mt-2 text-sm text-ink">{formatProbability(result.probability)}</p></div><div><p className="eyebrow text-[9px]">Risk level</p><div className="mt-2"><RiskBadge risk={result.risk_level} /></div></div><div><p className="eyebrow text-[9px]">Timestamp</p><p className="mono mt-2 text-[10px] text-ink">{result.timestamp ? formatDateTime(result.timestamp) : 'Just now'}</p></div></div></Card> }
