import { AlertTriangle, Binary, GitBranch, Percent, SlidersHorizontal, Table2 } from 'lucide-react'

const steps = [{ label: 'Student data', icon: Table2 }, { label: 'Feature engineering', icon: GitBranch }, { label: 'Preprocessing', icon: SlidersHorizontal }, { label: 'XGBoost', icon: Binary }, { label: 'Placement probability', icon: Percent }, { label: 'Risk interpretation', icon: AlertTriangle }]
export function ModelPipeline() { return <div className="flex flex-col gap-2 sm:flex-row sm:items-stretch sm:gap-0">{steps.map(({ label, icon: Icon }, index) => <div key={label} className="flex flex-1 items-center sm:items-stretch"><div className="flex min-h-16 flex-1 items-center gap-3 border border-line bg-recessed px-3 py-3 sm:flex-col sm:justify-center sm:gap-2 sm:text-center"><Icon aria-hidden="true" className={`h-4 w-4 shrink-0 ${index === 3 ? 'text-cyan' : 'text-meta'}`} /><span className="text-xs font-semibold text-ink">{label}</span></div>{index < steps.length - 1 && <span aria-hidden="true" className="hidden items-center px-1 text-cyan sm:flex">→</span>}</div>)}</div> }
